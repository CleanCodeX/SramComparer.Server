@using WebApp.SoE.Helpers

@attribute [Route(PageUris.Imagery)]
@inherits QueryMarkupPageBase

@Content

@code {
	protected override Task OnParametersSetAsync()
	{
		AutoTranslate = false;
		return HandleQueryParamAsync(nameof(PageUris.Imagery));
	}
}
