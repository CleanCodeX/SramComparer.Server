@using WebApp.SoE.Helpers

@attribute [Route(PageUris.WebChangelog)]
@inherits QueryMarkupPageBase

@Content

@code {
	protected override void OnParametersSet() => HandleQueryParam(PageUris.WebChangelog.Substring(1));
}
