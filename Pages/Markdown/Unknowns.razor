@using WebApp.SoE.Helpers

@attribute [Route(PageUris.Unknowns)]
@inherits QueryMarkupPageBase

@Content

@code {
	protected override Task OnParametersSetAsync() => HandleQueryParamAsync(nameof(PageUris.Unknowns));
}
