@using WebApp.SoE.Helpers

@attribute [Route(PageUris.Unknowns)]
@inherits QueryMarkupPageBase

@Content

@code {
	protected override void OnParametersSet() => HandleQueryParam(nameof(PageUris.Unknowns));
}
